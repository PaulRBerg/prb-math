div.t.sol
├── when one of the operands is zero
│  └── it should return zero
└── when neither of the operands is zero
   ├── when the result overflows
   │  └── it should revert
   └── when the result does not overflow
      └── it should return the correct value
